
spring:
  session:
    store-type: NONE
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/db/h2/fondue;AUTO_SERVER=TRUE;MODE=PostgreSQL;IGNORECASE=TRUE;DATABASE_TO_UPPER=TRUE
    username: sa
    password: 

server:
  contextPath: /fapp1
  session:
    timeout: 10800

logging:
  level:
    app: DEBUG
